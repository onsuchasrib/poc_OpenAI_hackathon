export const optionalOpenAiDisclosure = {
  coreDemo: 'The core demo is deterministic and works without an API key.',
  optionalVoice:
    'Optional live OpenAI voice would require a backend token endpoint using OPENAI_API_KEY; do not put standard API keys in browser code.',
  sponsoredConstraint: 'If an API is added, use OpenAI products only.',
};
